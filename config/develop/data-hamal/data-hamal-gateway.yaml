spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          url-expression: uri
      routes:
        - id: data-hamal-service-hello
          uri: lb://data-hamal-service-hello
          predicates:
            - Path=/hello/**

data-hamal:
  auth:
    whites:
      - /login
      - /register